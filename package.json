{
  "name": "email-signature",
  "version": "1.0.0",
  "description": "",
  "main": "src/javascript.js",
  "scripts": {
    "start": "npm run watch",
    "watch": "npx browser-sync start --server 'src' --files 'src'",
    "deploy": "node -e 'require(\"gh-pages\").publish(\"src\", err => {} );'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.12",
    "gh-pages": "^3.1.0"
  },
  "dependencies": {
    "clipboard": "^2.0.6"
  }
}
